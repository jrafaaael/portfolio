---
import Link from "./Link.astro";
import { links } from "../data/social-links";

const currentYear = new Date().getFullYear();
---

<footer
  class="max-w-6xl m-auto py-8 px-4 flex flex-col justify-between items-center gap-6 md:px-8 md:flex-row md:items-baseline"
>
  <p class="text-rose-100/70 text-sm order-2 md:-order-1">
    © {currentYear},
    <Link href="https://github.com/jrafaaael/">jrafaaael</Link>
  </p>
  <ul class="flex gap-6">
    {
      links.map(({ icon: Icon, link }) => (
        <li>
          <Link href={link}>
            <Icon />
          </Link>
        </li>
      ))
    }
  </ul>
</footer>
